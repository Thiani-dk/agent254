# .env.example — Template file. Do NOT include real credentials!

# Flask Secret Key (used for sessions and signing)
SECRET_KEY=replace_with_your_secret_key

# OTP Salt (used to hash OTPs uniquely for your app)
OTP_SALT=replace_with_your_otp_salt

# SMTP Settings (Used in "Chat with Colleagues" flow)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-app-specific-password

# Allowed colleague organization domains (comma-separated, no spaces)
ALLOWED_ORGS=strathmore.edu

# Base URL (used for generating retrieve links)
BASE_URL=https://agent254.onrender.com
